---
import Header from "../components/Header.svelte";
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Power Meter</title>
</head>
<body>
    <slot></slot>
</body>
</html>

<style>
    @font-face {
        font-family: "Dosis";
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("../fonts/Dosis.woff2") format("woff2");
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
    }
    
    :root {
        --main-color: hsl(191, 100%, 50%);
        --accent-color: hsl(191, 100%, 78%);
        --background-color: rgb(32, 32, 32);
        --background-color-dark: rgb(24, 24, 24);
        --contrast-color: rgb(255, 255, 255);
    }
    
    body {
        margin: 0;
        padding: 0;
        border: 0;
        font-family: "Dosis";
        background-color: var(--background-color);
    }

    :global(.bar) {
        z-index: 1;
        background-color: var(--main-color);
        box-shadow: 0 0 30px var(--background-color);
    }

    :global(.card) {
        background-color: var(--background-color-dark);
        width: clamp(100px, 85vw, 1000px);
        border-radius: 20px;
        --margin-top-bottom: 2vh;
        margin-top: var(--margin-top-bottom);
        margin-bottom: var(--margin-top-bottom);
    }
</style>