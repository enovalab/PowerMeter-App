---
import Header from "../components/Header.svelte";
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="manifest" href="../manifest.json"/>
    <link rel="shortcut icon" href="../icons/bolt_blue_24dp.svg" type="image/x-icon">
    <title>Power Meter</title>
</head>
<body>
    <slot></slot>
</body>
</html>

<style>
    @font-face {
        font-family: "Dosis";
        font-style: normal;
        font-weight: 400;
        font-display: swap;
        src: url("../fonts/Dosis.woff2") format("woff2");
        unicode-range: U+0000-00FF, U+0131, U+0152-0153, U+02BB-02BC, U+02C6, U+02DA, U+02DC, U+2000-206F, U+2074, U+20AC, U+2122, U+2191, U+2193, U+2212, U+2215, U+FEFF, U+FFFD;
    }
    
    :root {
        --main-color: rgb(0, 208, 255);
        --accent-color: rgb(143, 234, 255);
        --background-color: rgb(32, 32, 32);
        --background-color-dark: rgb(24, 24, 24);
        --contrast-color: rgb(255, 255, 255);
        --inactive-color: rgb(86, 86, 86); 
        --error-color: rgba(255, 0, 0);
    }
    
    body {
        width: 100dvw;
        height: 100dvh;
        height: ;
        margin: 0;
        padding: 0;
        border: 0;
        font-family: "Dosis";
        background-color: var(--background-color);
        user-select: none;
        color: var(--contrast-color);
    }
    
    :global(.bar) {
        z-index: 1;
        background-color: var(--main-color);
        box-shadow: 0 0 30px var(--background-color);
    }

    :global(.card) {
        background-color: var(--background-color-dark);
        width: min(90%, 1300px);
        max-width: min(90%, 1300px);
        border-radius: 20px;
        --margin-top-bottom: 2vh;
        margin-top: var(--margin-top-bottom);
        margin-bottom: var(--margin-top-bottom);
    }

    :global(.card-padding) {
        padding-top: 20px;
        padding-bottom: 20px;
    }

    :global(button) {
        border: 0;
        cursor: pointer;
    }
</style>